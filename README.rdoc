#Gender Guesser
Gender Guesser guesses your gender based on height and weight by using a classification algorithm. This app is meant to be a proof of concept than anything practical.

## Deployment instructions

Clone the project and navigate to the root directory. From terminal:

- bundle exec rake db:migrate
- bundle exec rake db:seed
- rails server

Navigate to [localhost:3000](http://localhost:3000)

